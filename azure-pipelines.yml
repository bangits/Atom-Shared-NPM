trigger:
- master

pool:
  vmImage: ubuntu-latest

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '14.x'
  displayName: 'Install Node.js'

- script: |
    npm install
  displayName: 'npm install'
  condition: ne(variables['CacheRestored'], 'true')

- task: Cache@2
  inputs:
    key: 'npm | “$(Agent.OS)” | $(Build.SourcesDirectory)/Common/FRONTEND/package-lock.json'
    path: '$(Build.SourcesDirectory)/Common/FRONTEND/node_modules'
    cacheHitVar: 'CacheRestored'
  
- script: |
    npm run build
  displayName: 'npm build'

- task: Npm@1
  inputs:
    command: 'custom'
    customCommand: 'run ci'
    publishRegistry: 'useFeed'
    publishFeed: '9c247408-f345-4df7-a08b-aa453b780d6f/d757e11d-d717-451f-be0a-43e0be7d96f7'
  env:
    GH_TOKEN: $(GH_TOKEN)
  displayName: 'Publishing package'